id: on_message
title: Red Button Handler
description: Handle both chat messages and webhook events
type: messaging
start: reply
schema_version: 2

nodes:
  reply:
    emit.response:
      messages:
        - type: text
          text: "{{in.input.text}}"
    routing:
      - out: true
